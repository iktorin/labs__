//Done by: Kaznienko Viktoria
// 123
// variant 10
/*Приклад ланцюжків, які не належать мові L(V):
1.*1234 (символ '*' на початку)
2.1234* (символ '*' в кінці)
3.abc*123 (літери, а не цифри)
4.123-456 (використання символу '-' замість '*')

Приклад ланцюжків, які належать мові L(V):
1.123*456
2.7888*4456
3.5*9*/
#include <iostream>  // Підключаємо бібліотеку для введення/виведення
#include <string>    // Підключаємо бібліотеку для роботи з рядками
#include <Windows.h> // для відоюраження кирилиці
using namespace std; // std для спрощення синтаксису

// Функція, яка перевіряє, чи є рядок дійсним згідно з мовою L(V)
bool isValidString(const string& word) {
    int len = word.size();  // Отримуємо довжину рядка
    bool result = true;     // Задаємо початкове припущення, що рядок є правильним

    // Знаходимо позицію символу '*' у рядку
    int starPos = word.find('*');

    // Перевіряємо, чи є символ '*' в рядку і чи не знаходиться він на початку або в кінці
    if (starPos == string::npos || starPos == 0 || starPos == len - 1) {
        return false;  // Якщо '*' не знайдено або він на початку чи в кінці, повертаємо false
    }

    // Перевіряємо першу частину рядка (до символу '*')
    for (int i = 0; i < starPos; ++i) {  // Проходимо по символах до '*'
        if (!isdigit(word[i])) {  // Якщо символ не є цифрою
            return false;  // Рядок недійсний, повертаємо false
        }
    }

    // Перевіряємо другу частину рядка (після символу '*')
    for (int i = starPos + 1; i < len; ++i) {  // Проходимо по символах після '*'
        if (!isdigit(word[i])) {  // Якщо символ не є цифрою
            return false;  // Рядок недійсний, повертаємо false
        }
    }

    return result;  // Якщо всі перевірки пройшли успішно, повертаємо true
}

int main() {
    SetConsoleCP(1251);//для відображення тексту на українській
    SetConsoleOutputCP(1251);

    setlocale(LC_ALL, "RU");
    string word;  // Оголошуємо змінну для введеного рядка

    // Виводимо запит для введення рядка
    cout << "Введіть рядок: ";
    cin >> word;  // Зчитуємо рядок від користувача

    // Викликаємо функцію isValidString для перевірки валідності рядка
    bool result = isValidString(word);

    // Виводимо результат перевірки
    if (result) {
        cout << "Рядок належить мові L(V)" << endl;  // Якщо рядок валідний, виводимо повідомлення
    }
    else {
        cout << "Рядок не належить мові L(V)" << endl;  // Якщо рядок не валідний, виводимо інше повідомлення
    }

    return 0;  
}

